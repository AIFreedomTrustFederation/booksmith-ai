# LaTeX production

Canonical LaTeX source lives under `../manuscript/`. Booksmith's renderer copies and rewrites that project into `exports/latex/`; generated export files are not hand-edited. Use `node scripts/render-latex.mjs towards-a-unified-reality` from the repository root.
